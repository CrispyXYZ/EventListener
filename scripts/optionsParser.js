import {Options} from './options.js';

var filtchat = Options.dontShowBadWords,
    filtlist = Options.badWordsList,
    enflycmd = Options.enableFlyCommand,
    emonster = Options.enhancedMonsters,
    invsneak = Options.invisibleSneaking,
    custocmd = Options.enableCustomCammands,
    cmdslist = Options.customCommands;

export {
filtchat,
filtlist,
enflycmd,
emonster,
invsneak,
custocmd,
cmdslist };